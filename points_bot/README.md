# 🤖 Bot Hosting Platform

**منصة استضافة بوتات Discord غير محدودة - مجاناً للأبد**

## ✨ المميزات

- ✅ إضافة عدد **لا محدود** من البوتات
- ✅ واجهة ويب سهلة وجميلة
- ✅ تشغيل/إيقاف/إعادة تشغيل البوتات
- ✅ مراقبة حالة البوتات مباشرة
- ✅ كتابة كود البوت من المتصفح
- ✅ إحصائيات لحظية
- ✅ حفظ تلقائي للإعدادات
- ✅ **مجاني تماماً**

## 🚀 التشغيل

### محلياً على جهازك:
```bash
cd web
python hosting-manager.py
```

ثم افتح: http://localhost:8080

### على Railway (24/7):
1. عدّل `Procfile` واضف:
   ```
   web: python web/hosting-manager.py
   ```
2. Railway سيشغله تلقائياً

## 📖 كيفية الاستخدام

### إضافة بوت جديد:
1. افتح الموقع
2. اكتب **اسم البوت**
3. اكتب **التوكن** (اختياري)
4. الصق **كود البوت**
5. اضغط "إضافة البوت"

### تشغيل بوت:
- اضغط زر "▶️ تشغيل"

### إيقاف بوت:
- اضغط زر "⏹️ إيقاف"

### إعادة تشغيل:
- اضغط زر "🔄 إعادة"

## 📂 هيكل الملفات

```
web/
├── hosting-manager.py      # المنصة الرئيسية
├── bots_config.json        # إعدادات البوتات
├── ai-bot-real.py          # بوت الإصلاح الذكي
└── your-bot.py             # البوتات التي تضيفها
```

## 🔒 الأمان

- التوكنات تُحفظ محلياً فقط
- كل بوت يعمل في عملية منفصلة
- إيقاف تلقائي عند الأخطاء
- لا يوجد حد لعدد البوتات

## 💡 أمثلة

### بوت بسيط:
```python
import discord
from discord.ext import commands

bot = commands.Bot(command_prefix='!')

@bot.event
async def on_ready():
    print(f'{bot.user} جاهز!')

@bot.command()
async def ping(ctx):
    await ctx.send('Pong!')

bot.run('YOUR_TOKEN')
```

### بوت متقدم:
```python
import discord
from discord.ext import commands
import os

TOKEN = os.getenv('MY_BOT_TOKEN')

intents = discord.Intents.default()
intents.message_content = True

bot = commands.Bot(command_prefix='-', intents=intents)

@bot.event
async def on_ready():
    print(f'🤖 {bot.user} online!')
    await bot.change_presence(activity=discord.Game('!help'))

@bot.command()
async def hello(ctx):
    await ctx.send(f'مرحباً {ctx.author.mention}!')

bot.run(TOKEN)
```

## 🌐 API Endpoints

### GET `/api/bots`
الحصول على قائمة البوتات

### POST `/api/bots/add`
إضافة بوت جديد
```json
{
  "name": "my-bot",
  "token": "TOKEN",
  "code": "PYTHON_CODE"
}
```

### POST `/api/bots/{name}/start`
تشغيل بوت

### POST `/api/bots/{name}/stop`
إيقاف بوت

### DELETE `/api/bots/{name}`
حذف بوت

## 🎯 الميزات القادمة

- [ ] رفع ملفات Python مباشرة
- [ ] لوقات مباشرة للبوتات
- [ ] جدولة التشغيل التلقائي
- [ ] دعم متغيرات البيئة المتعددة
- [ ] نسخ احتياطي تلقائي

## ⚡ نصائح

1. احفظ التوكنات في المتغيرات البيئية
2. استخدم `os.getenv()` في الكود
3. جرب البوت محلياً قبل الرفع
4. راقب حالة البوتات من الموقع

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من الكونسول
2. راجع ملف `bots_config.json`
3. جرب إعادة تشغيل المنصة

---

**استضافة غير محدودة | سهلة الاستخدام | مجانية 100%**
