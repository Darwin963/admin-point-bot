# 🤖 AI Bot - مساعد ذكاء اصطناعي

بوت ذكاء اصطناعي قوي لإصلاح المشاكل وإدارة المشروع تلقائياً - **مجاني للأبد**

## ✨ المميزات

- 🔧 إصلاح المشاكل تلقائياً
- 📝 كتابة أكواد برمجية
- 🔍 تحليل الملفات
- ⚡ تحسين الأداء
- 💾 نسخ احتياطي
- 🐛 تصحيح الأخطاء
- 🧠 التعلم الذاتي
- ⚙️ تنفيذ أكواد Python مباشرة

## 📋 الأوامر

### أوامر عامة
```
!ai help                    - قائمة الأوامر
!ai status                  - حالة النظام
!ai restart                 - إعادة تشغيل
```

### أوامر الإصلاح
```
!ai fix <وصف المشكلة>       - إصلاح أي مشكلة
!ai debug <وصف الخطأ>        - تصحيح الأخطاء
!ai optimize                - تحسين الأداء
```

### أوامر البرمجة
```
!ai code <لغة> <وصف>        - كتابة كود
!ai execute <كود>           - تنفيذ كود Python
!ai analyze <ملف>           - تحليل ملف
```

### أوامر إدارية
```
!ai backup                  - نسخة احتياطية
!ai learn <موضوع>           - تعلم شيء جديد
```

## 🚀 التشغيل

### 1. إنشاء بوت Discord جديد
1. اذهب إلى https://discord.com/developers/applications
2. اضغط "New Application"
3. اختر اسم: "AI Assistant Bot"
4. اذهب إلى "Bot" → "Add Bot"
5. انسخ الـ Token

### 2. ضبط الصلاحيات
في صفحة Bot، فعّل:
- MESSAGE CONTENT INTENT ✅
- SERVER MEMBERS INTENT ✅
- PRESENCE INTENT ✅

### 3. دعوة البوت للسيرفر
1. اذهب إلى "OAuth2" → "URL Generator"
2. اختر Scopes: `bot`
3. اختر Bot Permissions:
   - Administrator (أو حسب الحاجة)
4. انسخ الرابط وافتحه في المتصفح

### 4. تشغيل البوت

#### الطريقة الأولى: تشغيل محلي
```bash
cd "c:\Users\DELL\OneDrive\Desktop\Admin Point Bot\web"
python ai-bot.py
```

#### الطريقة الثانية: على Railway
1. أضف متغير بيئي في Railway:
   - `AI_BOT_TOKEN` = التوكن الذي نسخته
2. أضف هذا السطر في `Procfile`:
   ```
   ai: python web/ai-bot.py
   ```
3. سيعمل البوت مع البوت الرئيسي

## 🔐 الصلاحيات

المستخدمون المسموح لهم:
- ID: `739749692308586526`
- ID: `1020294577153908766`
- أي مستخدم لديه صلاحيات Administrator

لإضافة مستخدمين جدد، عدّل السطر:
```python
AI_ADMIN_IDS = {739749692308586526, 1020294577153908766, YOUR_ID_HERE}
```

## 💡 أمثلة الاستخدام

### إصلاح مشكلة
```
!ai fix البوت يتعطل عند إرسال رسالة
```

### كتابة كود Discord.py
```
!ai code discord.py أمر لحذف الرسائل
```

### تحليل ملف
```
!ai analyze bot.py
```

### تنفيذ كود مباشر (خطير!)
```
!ai execute
await ctx.send("Hello from AI!")
```

### تحسين الأداء
```
!ai optimize
```

### نسخة احتياطية
```
!ai backup
```

## ⚠️ تحذيرات

1. **أمر execute خطير جداً** - يمكنه تنفيذ أي كود Python
2. البوت حالياً يعطي ردود تجريبية - لإضافة ذكاء اصطناعي حقيقي:
   - استخدم OpenAI API
   - استخدم Google Gemini API
   - استخدم Hugging Face Models

## 🔮 ترقية للذكاء الحقيقي

لتحويله لبوت ذكاء اصطناعي حقيقي مجاني:

### استخدام Google Gemini (مجاني)
```bash
pip install google-generativeai
```

```python
import google.generativeai as genai

genai.configure(api_key="YOUR_GEMINI_API_KEY")
model = genai.GenerativeModel('gemini-pro')

@bot.command()
async def ask(ctx, *, question: str):
    response = model.generate_content(question)
    await ctx.send(response.text)
```

### الحصول على Gemini API (مجاني):
https://makersuite.google.com/app/apikey

## 📝 ملاحظات

- البوت يعمل 24/7 على Railway مجاناً
- جميع الأوامر آمنة ما عدا `execute`
- يمكن إضافة أوامر جديدة بسهولة
- الكود مفتوح المصدر - عدّل كما تشاء

## 🆘 الدعم

إذا واجهت مشاكل:
1. تأكد من التوكن صحيح
2. تأكد من تفعيل Intents
3. تأكد من صلاحيات البوت في السيرفر
4. استخدم `!ai debug` للتشخيص

---

**مجاني للأبد | قوي | سهل الاستخدام | آمن**
